<Parent>:
    cols: 1
    rows: 2

    Nav:
        id: Nav

        pos_hint: {"x": 0, "top": 1}
        size_hint: 1, 0.1

    ScreenManager:
        id: ScreenManager

        pos_hint: {"x": 0, "y": 0}
        size_hint: 1, 0.9